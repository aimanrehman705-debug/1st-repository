# WhatsX Frontend

## Setup

1. Copy `.env.example` to `.env` and fill values (backend URL and Firebase web config).
2. Install deps and run dev:

```bash
npm install
npm run dev
```

The app runs at http://localhost:5173.

## Notes
- Uses TailwindCSS with dark mode, Chart.js via react-chartjs-2, and Firebase Auth.
